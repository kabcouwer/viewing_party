<%= bootstrap_form_with model: @user, url: login_path, local: true do |form| %>
  <%= form.email_field :email, label: 'Email Address' %>
  <%= form.password_field :password, label: 'Password'  %>

  <%= form.submit 'Log In' %>
<% end %>
